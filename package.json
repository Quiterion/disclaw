{
  "name": "disclaw",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "slice0": "node --import tsx --env-file=.env src/slice0.ts",
    "daemon": "node --import tsx --env-file=.env src/daemon.ts",
    "ctl": "node --import tsx src/ctl.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
